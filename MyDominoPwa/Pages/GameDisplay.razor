@page "/Game"

@if (GlobalState.Over) {
	<Winners EndTournament="@StateHasChanged" />
} else if (GlobalState.Created) {
	<GameManager ParentStateHasChange="@StateHasChanged"/>
} else {
	<NotInitiatedGame/>
}